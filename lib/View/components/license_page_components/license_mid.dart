import 'package:flutter/material.dart';

class LicenseMid extends StatelessWidget {
  const LicenseMid({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: PageView(
        children: [],
      ),
    );
  }
}
